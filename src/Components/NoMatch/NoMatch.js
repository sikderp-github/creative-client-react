import React from 'react';

const NoMatch = () => {
    return (
        <div className="mt-5 ml-5">
            <h4 style={{ color: 'red' }}>Sorry, the page is not available now.</h4>
        </div>
    );
};

export default NoMatch;